<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <title>Contactame</title>
   <% include parciales/head %>
</head>

<body>
   <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
      <a class="navbar-brand" href="/">Inicio Curriculum
         <img class="animated bounce" src="/img/flechaizquierda.png" height="20px" width="20px" alt="">
         volver
      </a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse"
         aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
         <span class="navbar-toggler-icon"></span>
      </button>
   </nav>

   <div class="container p-5">
      <div class="row">
         <div class="col-sm-4 offset-md-4">
            <div class="card animated flipInY">
               <div class="card-header bg-dark text-white text-center">
                  <h3>Contactame</h3>
               </div>
               <div class="card-body">
                  <form action="send" method="POST">
                     <div class="form-group">
                        <input id="idInCE" name="correo" class="form-control" type="email" placeholder="Email" required>
                     </div>
                     <div class="form-group">
                        <textarea id="idInMen" class="form-control" name="mensaje" cols="30" rows="10"
                           placeholder="Mensaje"></textarea>
                     </div>
                     <div class="form-group">
                        <button type="submit" id="idBtnEnviar" class="btn btn-primary btn-block">
                           Enviar
                        </button>
                     </div>
                  </form>
               </div>
            </div>
         </div>
      </div>
   </div>

   <% include parciales/footer %>

   <!-- <script >
      
      document.getElementById("idBtnEnviar").addEventListener("click", function () {
         let strCorreo = document.getElementById("idInCE").value;
         let strMensaje = document.getElementById("idInMen").value;

         if (strCorreo != "" && strMensaje != "") {
            
         } else {
            alert("Por fovor rellenar todos los campos");
         }
      });
   </script> -->
   
</body>

</html>